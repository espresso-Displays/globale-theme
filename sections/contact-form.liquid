{% layout 'page' %}

<style>
  .contact-hero {
    background: url('{{ "contact-us.webp" | asset_url }}') center center / cover no-repeat;
    height: 300px;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .contact-hero::before {
    content: "";
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.4);
  }

  .contact-hero h1 {
    position: relative;
    color: white;
    font-size: 2.5rem;
    font-weight: bold;
    z-index: 1;
  }

  .contact-wrapper {
    max-width: 700px;
    margin: 0 auto;
    padding: 40px 20px;
  }

  .contact-intro {
    text-align: center;
    margin-bottom: 40px;
  }

  .contact-intro h2 {
    font-size: 1.75rem;
    font-weight: 600;
    margin-bottom: 10px;
  }

  .contact-intro p {
    font-size: 1rem;
    color: #555;
  }

  .contact-intro a {
    color: #0074cc;
    text-decoration: underline;
  }

  .contact-form label {
    display: block;
    font-weight: 600;
    margin-bottom: 6px;
  }

  .contact-form input,
  .contact-form textarea {
    width: 100%;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 4px;
    margin-bottom: 20px;
    font-size: 1rem;
  }

  .contact-form textarea {
    resize: vertical;
  }

  .contact-form button {
    background-color: #000;
    color: white;
    padding: 12px 24px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 1rem;
  }

  .contact-form button:hover {
    background-color: #333;
  }

  .form-message {
    padding: 16px;
    margin-bottom: 24px;
    border-radius: 4px;
  }

  .form-success {
    background-color: #e6f7e6;
    color: #2e7d32;
  }

  .form-error {
    background-color: #fdecea;
    color: #c62828;
  }
</style>

<section class="contact-hero">
  <h1>Get in Touch</h1>
</section>

<div class="contact-wrapper">
  <div class="contact-intro">
    <h2>We're here to help</h2>
    <p>
      Whether you're curious about features, partnerships, B2B sales, or press — we're ready to answer your questions.
      For support, please visit our <a href="/pages/support">Support site</a>.
    </p>
  </div>

  {% form 'contact' %}
    {% if form.posted_successfully? %}
      <div class="form-message form-success">Thanks for your message! We'll get back to you soon.</div>
    {% elsif form.errors %}
      <div class="form-message form-error">
        <p><strong>Please fix the following errors:</strong></p>
        <ul>
          {% for field in form.errors %}
            <li>{{ form.errors.messages[field] }}</li>
          {% endfor %}
        </ul>
      </div>
    {% endif %}

    <div class="contact-form">
      <label for="ContactFormName">Name</label>
      <input type="text" id="ContactFormName" name="contact[name]" required>

      <label for="ContactFormEmail">Email</label>
      <input type="email" id="ContactFormEmail" name="contact[email]" required>

      <label for="ContactFormSubject">Subject</label>
      <input type="text" id="ContactFormSubject" name="contact[subject]">

      <label for="ContactFormMessage">Message</label>
      <textarea id="ContactFormMessage" name="contact[body]" rows="6" required></textarea>

      <button type="submit">Send Message</button>
    </div>
  {% endform %}
</div>
<form id="custom-add-to-cart-form" action="/cart/add" method="post" class="">
  <input type="hidden" name="id" value="{{ product.variants.first.id }}">
  <input type="hidden" name="quantity" value="1">
  <input type="hidden" name="productTitle" value="{{ product.title }}">
  <input type="hidden" name="currency" value="{{ shop.currency }}">
  <input type="hidden" name="price" value="{{  product.variants.first.price| divided_by: 100  }}">
  <input type="hidden" name="productTitle" value="{{ product.title }}">
  <input type="hidden" name="currency" value="{{ shop.currency }}">
  <input type="hidden" name="price" value="{{  product.variants.first.price| divided_by: 100  }}">
  <button
    type="submit"
    class="z-999 rounded-[5px] text-center  bg-lm-primary text-lm-on-primary hover:bg-lm-primary-hover active:bg-lm-primary-click w-[150px] h-[48px] px-[16px] py-[8px] text-label-medium cursor-pointer "
  >
    Add to Cart
  </button>
</form>
